$kind: http-request
name: Get All Reservations
method: GET
url: '{{baseUrl}}/reservations'
order: 2000
description: Retrieve all reservations. Requires JWT authentication.
headers:
  - key: Cookie
    value: 'Authentication={{jwt}}'
