apiVersion: v1
kind: Service
metadata:
  name: notifications
spec:
  selector:
    app: notifications
  ports:
    - port: {{ .Values.notifications.env.PORT }}
      targetPort: {{ .Values.notifications.env.PORT }}
